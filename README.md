---

﻿# P0: RISC-V 汇编 — 累加求和 (1~50)

用 RISC-V 汇编实现 1 到 50 的累加求和，通过循环加法与跳转指令完成计算并输出结果。

**关键实现**
- 循环累加器模式，使用 `beq` 条件分支控制循环终止
- 通过 BIOS 跳转表调用串口输出结果
- Makefile 中集成 `riscv64-unknown-elf-gcc` 交叉编译与 QEMU 运行
